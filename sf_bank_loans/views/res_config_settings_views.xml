<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_bank_loans" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.bank.loans</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="50"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Bank Loans" string="Bank Loans" name="bank_loans">
                    <block title="Alerts" id="bank_loans_alerts">
                        <setting id="bank_loans_alert_days" help="Number of days after the due date before an unpaid installment raises an overdue alert.">
                            <field name="sf_loan_alert_days"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>