<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_haccp" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.haccp</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="50"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="HACCP Food Safety" string="HACCP Food Safety" name="haccp">
                    <block title="Alerts" id="haccp_alerts">
                        <setting id="haccp_alert_days" help="Number of tolerance days after the planned control date before a scheduled check without a result raises a reminder activity.">
                            <field name="sf_haccp_alert_days"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>